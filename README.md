# CK Join Flow extension plugin for LRU

This plugin handles updating existing GoCardless direct debit customers. It requires the CK Join Flow
plugin to work.

## Installation

Simply clone this repo in the WordPress plugins directory.